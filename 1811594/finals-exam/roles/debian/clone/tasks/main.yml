---
- name: Install git on debian
  apt:
   name: git
   state: latest

- name: Clone Prelim Exam branch on debian
  git:
    repo: '{{ prelims_exam }}'
    dest: ./prelim-exam
    version: prelim-exam

# tasks file for roles/debian/clone